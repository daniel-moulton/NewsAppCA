<?xml version="1.0" encoding="UTF-8" ?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search"
        android:title="Search"
        android:icon="@drawable/baseline_search_24"
        app:showAsAction="ifRoom"
        android:iconTint="@color/white"
        app:actionViewClass="android.widget.SearchView"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/settings"
        android:title="Settings"
        android:icon="@drawable/baseline_more_vert_24"
        app:showAsAction="always"
        tools:ignore="HardcodedText">
        <menu>
            <item
                android:id="@+id/searchFavQuery"
                android:title="Search For Saved Query "/>
            <item
                android:id="@+id/changeFavQuery"
                android:title="Change Saved Query"
                app:showAsAction="never"/>
            <item
                android:id="@+id/restartApp"
                android:title="Restart App"
                app:showAsAction="never"/>
            <item
                android:id="@+id/closeApp"
                android:title="Close App"
                app:showAsAction="never"/>
            <item
                android:id="@+id/visitWebsite"
                android:title="Visit My Website"
                app:showAsAction="never"/>
        </menu>
    </item>
</menu>
